{
  "name": "atlas-hub",
  "version": "1.0.0",
  "description": "Atlas Hub — AI Robot Presentation Site",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.169.0",
    "gsap": "^3.12.5",
    "lenis": "^1.1.14",
    "splitting": "^1.0.6",
    "vanilla-tilt": "^1.8.1"
  },
  "devDependencies": {
    "vite": "^5.4.10",
    "gh-pages": "^6.2.0"
  }
}
